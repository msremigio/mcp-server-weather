[project]
name = "mcp-weather"
version = "0.1.0"
description = "Fetch weather alerts and forecast data across the US"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
]
